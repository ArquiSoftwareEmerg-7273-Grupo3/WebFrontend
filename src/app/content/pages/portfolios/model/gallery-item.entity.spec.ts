import { GalleryItemEntity } from './gallery-item.entity';

describe('GalleryItemEntity', () => {
  it('should create an instance', () => {
    expect(new GalleryItemEntity()).toBeTruthy();
  });
});
